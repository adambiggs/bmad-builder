# {skill-description}

# Core identification
bmad-type: {bmad-type}
{if-module}bmad-module-name: {module-display-name}
bmad-module-code: {module-code}
{/if-module}
{if-replaces}replaces: {replaced-skill-name}
{/if-replaces}
# Phase context
bmad-phase: {phase}
bmad-required: {required}

# Execution relationships
bmad-requires: [{requires-list}]
bmad-prefer-after: [{prefer-after-list}]
bmad-prefer-before: [{prefer-before-list}]

# Output tracking
bmad-creates: {creates-description}
bmad-output-location-variable: {output-location-variable}
